# My bash setup on Mac

Including vim and bash setup in 

## instruction:
sudo git clone https://github.com/lorinma/dockeride.git /usr/src/dockeride; ln -sf
/usr/src/dockeride/.bashrc ~/.bashrc; ln -sf /usr/src/dockeride/.vim ~/.vim; ln -sf
/usr/src/dockeride/.vimrc ~/.vimrc; sudo curl -o miniconda.sh
https://repo.continuum.io/miniconda/Miniconda3-latest-MacOSX-x86_64.sh; sudo bash miniconda.sh -b
-p $HOME/miniconda; sudo rm -rf miniconda.sh; source ~/.bash_profile;
